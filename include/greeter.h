#ifndef SKELETON_GREETER_H
#define SKELETON_GREETER_H

#include <stddef.h>

/* Writes "Hello from <name>!" into buf and returns buf. */
char *greeting(char *buf, size_t buf_len, const char *name);

#endif /* SKELETON_GREETER_H */
